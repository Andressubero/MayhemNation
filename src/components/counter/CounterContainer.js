import React from 'react'
import {Counter} from './Counter'

export const CounterContainer = ({stock}) => {
    return (
        <div>
            <Counter stock={stock} />
            
        </div>
    )
}
